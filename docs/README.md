# Docs

This directory stores project documentation that should remain under version control.

## Current layout

- `superpowers/specs/`: design notes and planning documents generated during implementation.

## Notes

- Runtime logs are not stored here.
- Large raw data files remain under `reports_raw/`.
- Parsed outputs should remain under `parsed_csv/`.
